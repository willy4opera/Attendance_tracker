// This is a temporary file to show the changes needed
// Replace function signatures like:
// getTaskById(id: number)
// With:
// getTaskById(id: ID)

// And in the function body, replace:
// `/tasks/${id}`
// With:
// `/tasks/${formatIdForApi(id)}`
